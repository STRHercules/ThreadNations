[package]
name = "threadnations-persistence"
version.workspace = true
edition.workspace = true

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
threadnations-simulation = { path = "../simulation" }

[lints]
workspace = true
